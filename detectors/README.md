# Detectors Module
**Owner:** Member 2 (Features, Detection & ML)
**Scope:** Deterministic heuristic baselines, lightweight ML classifiers (LightGBM, Random Forest), and unsupervised anomaly detectors for all 6 required threat classes + novel anomalies.
